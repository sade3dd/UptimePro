{
	"name": "twilight-math-b4f0",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"build": "esbuild src/index.ts --bundle --minify --sourcemap --platform=browser --target=es2022 --outfile=dist/worker.js --external:cloudflare:workers/* --external:cloudflare:workers",
		
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@cloudflare/workers-types": "^4.20260329.1",
		"typescript": "^5.5.2",
		"wrangler": "^4.78.0"
	},
	"dependencies": {
		"hono": "^4.12.9"
	}
}
